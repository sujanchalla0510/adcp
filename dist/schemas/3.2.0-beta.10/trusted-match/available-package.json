{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://adcontextprotocol.org/schemas/3.2.0-beta.10/trusted-match/available-package.json",
  "title": "Available Package",
  "description": "A package available for contextual matching. Synced to providers at media buy creation time — not sent per request. Providers cache this metadata and use it when evaluating context match requests for the placement. The `seller_agent` field binds the package to the originating seller agent; providers MUST use this binding rather than re-deriving seller identity from media_buy_id.",
  "x-status": "experimental",
  "type": "object",
  "properties": {
    "package_id": {
      "type": "string",
      "description": "Unique identifier for the package"
    },
    "media_buy_id": {
      "type": "string",
      "description": "Media buy that this package belongs to"
    },
    "seller_agent": {
      "$ref": "https://adcontextprotocol.org/schemas/3.2.0-beta.10/core/seller-agent-ref.json",
      "description": "The seller agent that owns this package. `agent_url` MUST match one of `authorized_agents[].url` in the publisher's adagents.json authoritative for every property this package may serve. Providers SHOULD validate at sync time and reject mismatches with `seller_not_authorized`. Cached alongside the package and used for offer attribution, per-seller observability, and dispute resolution — not for request-time filtering."
    },
    "format_ids": {
      "type": "array",
      "deprecated": true,
      "description": "Deprecated in AdCP 3.2; removed in AdCP 4.0. Legacy named-format identifiers eligible for this package. Use canonical `format_options`.",
      "items": {
        "$ref": "https://adcontextprotocol.org/schemas/3.2.0-beta.10/core/format-id.json"
      }
    },
    "format_options": {
      "type": "array",
      "description": "Immutable seller-resolved PackageFormatSnapshots eligible for this package. A sender projects this field only when the Trusted Match transport profile supports the highest AdCP release version of every field in every snapshot. It MUST NOT strip an unsupported selector or digest field from a snapshot; for an older or version-unknown provider, omit the entire contract-bearing snapshot and do not claim production-path evidence from a legacy projection.",
      "items": {
        "$ref": "https://adcontextprotocol.org/schemas/3.2.0-beta.10/core/package-format-snapshot.json"
      },
      "minItems": 1,
      "x-adcp-validation": {
        "verifier_constraints": {
          "protocol_version": "the Trusted Match transport profile supports the highest AdCP release version of every field in each projected snapshot",
          "authority_stripping": "an unsupported contract-bearing snapshot is omitted whole; a sender never removes individual selectors, execution versions, or digests from an immutable snapshot"
        }
      }
    },
    "catalogs": {
      "type": "array",
      "description": "The buyer's catalogs attached to this package, with selectors (ids, gtins, tags, category, query) scoping which items are in play. References synced catalogs by catalog_id. The provider resolves items from its cached copy.",
      "items": {
        "$ref": "https://adcontextprotocol.org/schemas/3.2.0-beta.10/core/catalog.json"
      }
    }
  },
  "required": [
    "package_id",
    "media_buy_id",
    "seller_agent"
  ],
  "additionalProperties": false
}
